# SOURCE_REFRESH.md

## Purpose

Living validation log for durable vendor/repo facts and explicit unknowns.

## Verification Log

| Date/Time (UTC) | Source Area | Fact | Confidence | Implementation Impact | Code Changes Needed |
|---|---|---|---|---|---|
| 2026-03-12T07:25:00Z | Evidence-bundle authoritative metadata parity lifecycle proof | Added integration-style lifecycle assertion that persisted authoritative PAN-OS observed-fact metadata (`detail.rule_metadata`) is preserved unchanged in `/api/v1/requests/{id}/result/evidence-bundle` with parity to normal `/result` output, alongside readiness summary/details. | HIGH | Confirms evidence-bundle payload parity for authoritative observed-fact metadata without schema expansion or semantics changes. | YES (lifecycle tests + tracker/docs) |
| 2026-03-12T07:05:00Z | Evidence-bundle readiness parity lifecycle proof | Added integration-style lifecycle assertion that `/api/v1/requests/{id}/result/evidence-bundle` preserves persisted `source_readiness_summary` and `source_readiness_details` with parity to normal result retrieval for mixed explicit/fallback/degraded readiness states. | HIGH | Confirms evidence-bundle payload already carries existing readiness shaping fields end-to-end without result schema expansion or readiness-rule changes. | YES (lifecycle tests + tracker/docs) |
| 2026-03-12T06:25:00Z | Lifecycle non-dict readiness-shape handling | Added integration-style lifecycle proof that non-dict `source_readiness` entries are surfaced as `unknown_sources` in summary and excluded from details output. During proof, a real crash path was found in `ReadinessReport.available_sources`/`any_available` for non-dict entries and fixed via minimal dict guards. | HIGH | Confirms end-to-end summary/details behavior for malformed readiness shapes and hardens worker lifecycle safety without changing readiness probe logic or persistence schema. | YES (lifecycle test + tiny worker guard + tracker/docs) |
| 2026-03-12T04:05:00Z | Lifecycle readiness-details meaningful-entry filtering | Added integration-style lifecycle proof that persisted `source_readiness_details` excludes entries with no meaningful readiness keys (`status`, `reason`, `available`, `latency_ms`) while valid explicit/fallback entries remain present; summary output remains stable for valid availability signals. | HIGH | Confirms details filtering behavior is durable end-to-end through submit -> queue -> worker -> persist -> API retrieval without changing readiness probes or result schema. | YES (lifecycle tests + tracker/docs) |
| 2026-03-12T03:45:00Z | Lifecycle readiness fallback-status survival | Added integration-style lifecycle proof that fallback readiness status derivation survives persistence and API retrieval: sources with `available=true`/`available=false` and no explicit `status` are surfaced as `ready`/`unavailable` in `source_readiness_details`, while an entry with neither `available` nor `status` surfaces as `unknown`. | HIGH | Confirms fallback status rules are stable end-to-end in persisted result shaping (`source_readiness_summary` + `source_readiness_details`) without changing readiness collection logic or API schema. | YES (lifecycle tests + tracker/docs) |
| 2026-03-12T03:20:00Z | Lifecycle persistence of source readiness shaping | Added integration-style lifecycle proof that mixed per-source readiness states (`ready`, `not_configured`, `auth_failed`, `timeout`) survive submit -> queue -> worker -> persist -> `GET /api/v1/requests/{id}/result` and are surfaced as normalized `source_readiness_summary` plus `source_readiness_details`. | HIGH | Confirms readiness explainability fields are durable through the persisted worker/API lifecycle for realistic mixed states without changing readiness probe logic, classifier behavior, or result schema shape. | YES (lifecycle tests + tracker/docs) |
| 2026-03-12T02:55:00Z | SCM adapter normalization coverage audit + consolidation | Audited SCM normalization fail-closed critical-field coverage and found remaining meaningful gaps in `port` and `timestamp` field handling. Added one consolidated table-driven adapter unit batch proving deny-like authoritative candidates are dropped for missing/malformed `port` and missing `timestamp`. | HIGH | Breaks one-field-per-commit loop while completing remaining critical-field fail-closed coverage in one bounded pass; no runtime behavior changes were required. | YES (SCM adapter tests + tracker/docs) |
| 2026-03-12T02:25:00Z | SCM adapter normalization malformed timestamp type fail-closed proof | Added SCM adapter unit case proving an otherwise deny-like authoritative candidate with valid SCM/Strata source marker, deny decision, destination, and port is dropped when `timestamp` is present but object-typed/non-string. | HIGH | Tightens adapter-level shape guard so non-string timestamp values cannot normalize into authoritative SCM deny evidence. | YES (SCM adapter tests + tracker/docs) |
| 2026-03-12T02:10:00Z | SCM adapter normalization malformed destination type fail-closed proof | Added SCM adapter unit case proving an otherwise deny-like authoritative candidate with valid SCM/Strata source marker, deny decision, and port/timestamp is dropped when `destination` is present but object-typed/unusable. | HIGH | Tightens adapter-level context-match guard so non-string destination values cannot normalize into authoritative SCM deny evidence. | YES (SCM adapter tests + tracker/docs) |
| 2026-03-12T01:55:00Z | SCM adapter normalization blank destination fail-closed proof | Added SCM adapter unit case proving an otherwise deny-like authoritative candidate with valid SCM/Strata source marker, deny decision, destination field present as whitespace-only, and port/timestamp is dropped during normalization. | HIGH | Tightens adapter-level context-match guard so blank destination values cannot normalize into authoritative SCM deny evidence. | YES (SCM adapter tests + tracker/docs) |
| 2026-03-12T01:35:00Z | SCM adapter normalization missing destination fail-closed proof | Added SCM adapter unit case proving an otherwise deny-like authoritative candidate with valid SCM/Strata source marker, deny decision, port, and timestamp is dropped when destination field is missing. | HIGH | Tightens adapter-level context-match guard so destination is required before SCM deny-like records can normalize into authoritative evidence. | YES (SCM adapter tests + tracker/docs) |
| 2026-03-12T01:20:00Z | SCM adapter normalization invalid source marker proof | Added SCM adapter unit case proving an otherwise deny-like authoritative candidate with destination/port/timestamp and deny decision is dropped when `source_system` is present but not SCM/Strata/Prisma-equivalent. | HIGH | Extends fail-closed adapter-level authority proof from missing source marker to explicitly invalid source marker without runtime changes. | YES (SCM adapter tests + tracker/docs) |
| 2026-03-12T01:05:00Z | SCM adapter normalization fail-closed source marker proof | Added SCM adapter unit case proving an otherwise deny-like authoritative candidate with destination/port/timestamp and deny decision is dropped when source-of-record marker is missing. | HIGH | Tightens adapter-level assurance that SCM deny authority cannot be inferred without explicit SCM/Strata/Prisma source marker semantics. | YES (SCM adapter tests + tracker/docs) |
| 2026-03-12T00:50:00Z | SCM lifecycle malformed-shape fail-closed proof | Added one bounded negative lifecycle integration case where SCM emits a deny-like candidate with malformed decision structure (object decision, no usable deny action). The record is dropped before authoritative deny evidence persists, and retrieved result remains non-denied. | HIGH | Confirms end-to-end fail-closed behavior for malformed SCM evidence shape through submit -> queue -> worker -> persist -> API result retrieval, without runtime or SCM feature changes. | YES (lifecycle test + tracker/docs) |
| 2026-03-12T00:35:00Z | SCM lifecycle fail-closed proof | Added one bounded negative lifecycle integration case where SCM emits a deny-like candidate record with `authoritative=false`; authoritative correlation drops it, persisted result remains retrievable, and verdict stays non-denied. | HIGH | Confirms end-to-end fail-closed authority handling for SCM through the normal persistence/API retrieval path, complementing the existing positive deny/decrypt lifecycle proofs. | YES (lifecycle test + tracker/docs) |
| 2026-03-12T00:20:00Z | SCM lifecycle integration proof | Extended existing integration-style lifecycle harness to prove authoritative SCM deny and authoritative SCM decrypt-deny records survive submit -> queue -> worker -> persist -> API result retrieval with mock-only SCM adapter responses. | HIGH | Confirms the bounded SCM authoritative evidence path is not only unit-validated but also durable through normal persisted lifecycle retrieval, without SCM orchestration expansion or runtime logic changes. | YES (lifecycle test + tracker/docs) |
| 2026-03-12T00:05:00Z | SCM bounded authoritative evidence retrieval | Implemented SCM adapter bounded evidence path: one authenticated retrieval request against configured SCM API URL, strict normalization of clearly authoritative SCM deny/decrypt records only, and fail-closed behavior for auth/transport failures, malformed payloads, ambiguous/non-authoritative records, and non-deny decisions. | HIGH | Enables worker authoritative-correlation to consume real SCM authoritative deny/decrypt evidence safely without broad SCM orchestration/pagination expansion or runtime authority drift. | YES (SCM adapter + adapter tests + correlation tests/docs/tracker) |
| 2026-03-11T23:20:00Z | Authoritative correlation SCM gating hardening | Updated authoritative-correlation normalization to treat SCM like PAN-OS for deny authority filtering: only explicitly authoritative SCM deny records and authoritative decrypt-error records pass through; SCM allow or non-authoritative SCM records are dropped before classification. | HIGH | Tightens MVP deny-authority invariant and reduces false-deny risk without reopening PAN-OS token work or changing adapter readiness logic. | YES (worker step + unit tests + tracker/docs) |
| 2026-03-11T23:20:00Z | Unknown explainability copy follow-up status | Unknown-copy wording follow-up is parked pending real operator feedback; structured feedback ledger currently has no operator entries. | HIGH | Prevents speculative wording churn and enforces evidence-based copy iteration discipline. | YES (tracker note only) |
| 2026-03-11T23:00:00Z | Unknown explainability feedback capture workflow | Added repo-owned feedback recorder `scripts/record_unknown_explainability_feedback.py` and baseline artifacts `docs/review/UNKNOWN_EXPLAINABILITY_FEEDBACK.json` + `.md`. Workflow validates sample IDs against `UNKNOWN_EXPLAINABILITY_SAMPLES.json`, supports partial entries, appends machine-readable feedback rows, and summarizes per-sample aggregates plus copy-follow-up priority buckets. | HIGH | Converts operator wording feedback into a consistent, sortable, diffable artifact for bounded copy-only follow-up prioritization without touching runtime verdict/classifier/readiness behavior. | YES (script + review artifacts + docs/tracker) |
| 2026-03-11T22:30:00Z | Unknown explainability operator-review workflow | Added repo-owned generator `scripts/build_unknown_explainability_review.py` that scans persisted `ResultRow` unknown fixtures in `tests/routes/test_api_routes.py` and emits `docs/review/UNKNOWN_EXPLAINABILITY_SAMPLES.json` + `docs/review/UNKNOWN_EXPLAINABILITY_REVIEW.md` with per-sample context, derived/rendered reason signals, dominant-cause classification, and feedback rubric/template. | HIGH | Makes unknown-wording feedback collection cheap, structured, and traceable to real persisted unknown-result examples without changing verdict/classifier/readiness behavior. | YES (script + review artifacts + docs/tracker) |
| 2026-03-11T22:00:00Z | API/UI unknown explainability wording | Tightened `unknown_reason_signals` and result-page unknown-section copy to make root causes clearer (missing authoritative deny evidence, degraded readiness, low path confidence, inconclusive bounded checks) and to explicitly state that `unknown` is not equivalent to `allowed`. | HIGH | Improves operator interpretation of `unknown` without changing verdict logic, classifier behavior, source readiness collection, or adapter probes. | YES (API template/routes tests/docs/tracker) |
| 2026-03-11T21:10:00Z | LogScale adapter readiness probe hardening | Implemented bounded LogScale readiness probe in adapter via one repository endpoint request with short timeout/no retries. Readiness now emits explicit states in `source_readiness`: `ready`, `not_configured`, `auth_failed`, `unauthorized`, `unreachable`, `timeout`, `unexpected_response`, `internal_error`. | HIGH | Replaces ambiguous LogScale readiness with concrete diagnostics that flow into persisted `source_readiness` and existing `source_readiness_summary`/`source_readiness_details` surfacing, without expanding LogScale query/evidence retrieval or changing enrichment-only/non-authoritative boundaries. | YES (adapter/readiness tests/docs/tracker) |
| 2026-03-11T20:45:00Z | Torq adapter readiness probe hardening | Implemented bounded Torq readiness probe in adapter via one authenticated request to configured Torq API base URL with short timeout/no retries. Readiness now emits explicit states in `source_readiness`: `ready`, `not_configured`, `auth_failed`, `unauthorized`, `unreachable`, `timeout`, `unexpected_response`, `internal_error`. | HIGH | Replaces ambiguous Torq TODO readiness with concrete diagnostics that flow into persisted `source_readiness` and existing `source_readiness_summary`/`source_readiness_details` surfacing, without Torq workflow triggering/polling expansion or classifier/verdict changes. | YES (adapter/readiness tests/docs/tracker) |
| 2026-03-11T20:20:00Z | SD-WAN adapter readiness probe hardening | Implemented bounded SD-WAN readiness probe in adapter via one authenticated request to configured SD-WAN API base URL with short timeout/no retries. Readiness now emits explicit states in `source_readiness`: `ready`, `not_configured`, `auth_failed`, `unauthorized`, `unreachable`, `timeout`, `unexpected_response`, `internal_error`. | HIGH | Replaces ambiguous SD-WAN TODO readiness with concrete diagnostics that flow into persisted `source_readiness` and existing `source_readiness_summary`/`source_readiness_details` surfacing, without SD-WAN evidence-query expansion or classifier/verdict changes. | YES (adapter/readiness tests/docs/tracker) |
| 2026-03-11T20:00:00Z | Result-page readiness detail surfacing | Added compact "Source readiness details" UI block driven by additive `DiagnosticResult.source_readiness_details` derived from persisted `report_json.source_readiness`. Each entry exposes `source`, normalized `status`, optional `reason`, and optional `latency_ms`, with graceful fallback when readiness data is missing/malformed. | HIGH | Operators can now immediately see why sources (for example SCM) are unavailable without changing readiness collection, classifier logic, or verdict behavior. | YES (API load shaping + UI template + route tests + docs/tracker) |
| 2026-03-11T19:40:00Z | SCM adapter readiness probe hardening | Implemented bounded SCM readiness probe in adapter via single auth-token request with short timeout/no retries. Readiness now emits explicit states in `source_readiness`: `ready`, `not_configured`, `auth_failed`, `unauthorized`, `unreachable`, `timeout`, `unexpected_response`, `internal_error`. Worker readiness step now delegates SCM not-configured/probe diagnostics to adapter boundary. | HIGH | Replaces ambiguous SCM TODO readiness with concrete diagnostics that flow into persisted `source_readiness` and existing `source_readiness_summary` surfacing, without expanding SCM policy/log/evidence retrieval or changing classifier/verdict logic. | YES (adapter/readiness tests/docs/tracker) |
| 2026-03-11T19:05:00Z | API/UI result readiness surfacing | Added `source_readiness_summary` to `DiagnosticResult` load/response path, derived from persisted `report_json.source_readiness`, and rendered a compact "Source readiness" section in result UI. Summary includes `total_sources`, `available_sources`, `unavailable_sources`, and `unknown_sources`. | HIGH | Improves operator triage for `unknown` and partial-evidence outcomes without changing classifier/verdict logic or reopening PAN-OS token-expansion loops. | YES (models/api template/tests/docs/tracker) |
| 2026-03-11T18:35:00Z | PAN-OS candidate-family selector hardening | Added offline selector `scripts/select_next_panos_candidate.py` and generated `NEXT_CANDIDATE_DECISION.json` + `.md` from existing coverage/artifacts. Selector now classifies families as `proven`, `candidate`, `exhausted_pending_new_evidence`, or `blocked_by_loop_breaker`, and emits exactly one primary recommendation. Current output marks `10.1.99.3|10.1.20.21|30053|not-applicable|unknown|interzone-default|policy-deny|ssh_custom_command` as exhausted due to repeated no-hit evidence including strong-correlation input run. | HIGH | Encodes "stop retrying exhausted families" directly in repo-owned artifacts so future live attempts are selector-driven; if no viable non-exhausted candidate exists, action is to pause PAN-OS token expansion. | YES (script/tests/artifacts/docs/tracker) |
| 2026-03-11T16:48:00Z | PAN-OS bounded distinct-signature run with ready observability input (`11.0.6-h1`) | Prepared `docs/fixtures/panos_verification/OBSERVABILITY_INPUT.json` via `scripts/prepare_panos_observability_input.py` with strong machine-readable correlation fields (`session_id=0`, exact `ui_filter_string`, structured-row origin) and `ready_for_orchestrator=true`. Executed exactly one orchestrator run with `--observability-input`: `deny-hit-tcp-distinct-observe-validate-obsinput-stage1_20260311T164815Z`. Source-of-truth artifacts report `observability_hit=false`, `matched_entry_count=0`, `reason_if_not_validated=no_qualifying_deny_row`, no Stage 2 token subqueries, and no token validations. | HIGH | Confirms the stronger-input gate path works without bypass: loop-breaker allowed this one improved-correlation attempt, but result remains no-hit. No new token promotion justified; broader distinct-scenario destination-token behavior remains `UNVERIFIED`. | YES (generated artifacts + docs/tracker only) |
| 2026-03-11T18:15:00Z | PAN-OS observability-input hardening | Added repo-owned normalization helper `scripts/prepare_panos_observability_input.py` that ingests manual fields or single-row CSV/JSON UI exports and writes machine-readable `OBSERVABILITY_INPUT.json` with readiness gating (`ready_for_orchestrator`, `why_not_ready`) and confidence scoring. `scripts/panos_observe_and_validate.py` now accepts `--observability-input`, fails closed when provided input is not ready, and requires ready observability input for repeated materially identical no-hit retries. | HIGH | Encodes the postmortem decision into tooling: repeated low-confidence no-hit retries are blocked unless correlation evidence materially improves via `OBSERVABILITY_INPUT.json`; markdown template remains optional support only. No runtime classifier/API/UI behavior changed. | YES (scripts/tests/docs/tracker only) |
| 2026-03-11T16:30:00Z | PAN-OS evidence postmortem (`docs/fixtures/panos_verification/versions/**`) | Analysis-only coverage pass inspected all existing versioned `CAPTURE_METADATA.txt`, `OBSERVABILITY_RECORD.json`, and `VALIDATION_RESULT.json` artifacts and generated summary outputs: `OBSERVABILITY_COVERAGE.json` + `OBSERVABILITY_COVERAGE.md`. Classification outcome: one scenario-scoped proven token-validation path (`11.0.6-h1` UDP obsgate Stage 2 `addr.dst` + `dport`), one observability-hit-but-token-not-proven scenario (`deny-hit-udp-obsgate-stage1`), and repeated no-hit families (notably distinct signature `src=10.1.99.3 -> dst=10.1.20.21:30053` and replay signature family `src=10.1.99.10` + `rule=interzone-default` + `session_end_reason=policy-deny`). | HIGH | Explicitly breaks retry loop behavior for repeated no-hit families: next path is to require materially stronger observability correlation evidence before any new live PAN-OS attempt. No new token promotions or runtime logic changes were made. | YES (analysis script + docs/tracker/artifacts only) |
| 2026-03-11T15:13:00Z | PAN-OS bounded distinct-signature orchestrator retry (`11.0.6-h1`) | Executed exactly one additional distinct-signature run via `scripts/panos_observe_and_validate.py` with improved correlation input (`--session-id 0`, `--ui-filter-string "!( action eq 'allow' )"`, manual template supplement). Result artifacts: `deny-hit-tcp-distinct-observe-validate-stage1_20260311T151256Z/OBSERVABILITY_RECORD.json` and `VALIDATION_RESULT.json`. Outcome: loop-breaker allowed run (`blocked=false`, no prior matching `OBSERVABILITY_RECORD.json`), but `observability_hit=false`, `matched_entry_count=0`, `reason_if_not_validated=no_qualifying_deny_row`; no Stage 2 token subqueries executed. | HIGH | No new destination-token proof; distinct-scenario extension remains unverified. Future retries for same signature must use new/corrected correlation evidence and the latest `OBSERVABILITY_RECORD.json` loop-breaker state. | YES (fixtures/docs/tracker only) |
| 2026-03-11T08:10:00Z | PAN-OS orchestration state hardening | `scripts/panos_observe_and_validate.py` now writes `OBSERVABILITY_RECORD.json` for every run outcome (success, no-hit, SSH unavailable, loop-breaker blocked, capture failure) and uses attempt-signature loop-breaking to block repeated materially identical no-hit retries unless correlation input improves. `LIVE_DENY_OBSERVABILITY_TEMPLATE.md` is now optional manual supplement. | HIGH | Removes manual-template hard dependency for every attempt and makes machine-recorded observability/gating the default source of truth for bounded retries. | YES (orchestrator/tests/docs/tracker) |
| 2026-03-11T07:33:00Z | PAN-OS bounded distinct-signature observe-and-validate run (`11.0.6-h1`) | Executed exactly one live bounded run of `scripts/panos_observe_and_validate.py` for distinct TCP deny signature (`src=10.1.99.3`, `dst=10.1.20.21`, `dport=30053`, `app=not-applicable`, `rule=interzone-default`, `action=deny`, `session_end_reason=policy-deny`, zones `management->servers`). Source of truth was `VALIDATION_RESULT.json` in `deny-hit-tcp-distinct-observe-validate-stage1_20260311T073259Z`: `observability_hit=false`, `matched_entry_count=0`, `reason_if_not_validated=no_qualifying_deny_row`; Stage 2 token subqueries were not run. | HIGH | No additional destination-token behavior was proven beyond the original UDP scenario. Distinct-scenario extension for `addr.dst`/`dport` remains `UNVERIFIED`; no runtime-token promotion justified. | YES (fixtures/tracker/docs only) |
| 2026-03-11T07:05:00Z | PAN-OS one-shot orchestration workflow | Added repo-owned bounded orchestrator `scripts/panos_observe_and_validate.py` that automates source-host traffic generation over SSH, broad Stage 1 deny observability sweep, freshest-row selection, conditional Stage 2 token subqueries (`addr.dst`, `dport`), and writes machine-readable `VALIDATION_RESULT.json` in the Stage 1 capture directory. Orchestrator delegates PAN-OS calls to `gather_panos_fixtures.sh`, preserving read-only guardrails. | HIGH | Breaks repeated manual loop into one bounded run while keeping fail-closed behavior: SSH-unavailable stops with exact operator traffic command, no Stage 1 hit stops before Stage 2, and no auto-promotion is performed. | YES (script/tests/docs/tracker) |
| 2026-03-11T06:44:34Z | PAN-OS bounded distinct-signature XML verification (`11.0.6-h1`, fresh replay) | Executed one additional observability-gated Stage 1 real-capture run from a freshly updated distinct deny row (`src=10.1.99.3`, `dst=10.1.20.21`, `dport=30053`, `app=not-applicable`, `action=deny`, `rule=interzone-default`, `session_end_reason=policy-deny`, zones `management->servers`, type `drop`) with 15m receive-time bound and no destination-token clauses. Submit/poll succeeded (`job 472`, `FIN`) but poll returned `logs count=0`; Stage 2 was not run. Fixture: `deny-hit-tcp-distinct-stage1_20260311T064433Z`. | HIGH | No new token extension proof. `addr.dst`/`dport` remain proven only for the original obsgate UDP scenario pair; broader behavior remains `UNVERIFIED`. | YES (fixtures/docs/tracker only) |
| 2026-03-11T06:36:53Z | PAN-OS bounded distinct-signature XML verification (`11.0.6-h1`) | Executed one additional observability-gated Stage 1 real-capture run for a distinct deny signature from template row (`src=10.1.99.3`, `dst=10.1.20.21`, `dport=30053`, `app=not-applicable`, `rule=interzone-default`, `action=deny`, `session_end_reason=policy-deny`, zones `management->servers`) with 15m receive-time bound and no destination-token clauses. Submit/poll succeeded (`job 465`, `FIN`) but poll returned `logs count=0`; Stage 2 was not run. Fixture: `deny-hit-udp-distinct-stage1_20260311T063652Z`. | HIGH | Does not extend proof beyond the original UDP scenario. `addr.dst`/`dport` remain proven only for `deny-hit-udp-obsgate-stage1_20260311T052621Z` + `deny-hit-udp-obsgate-stage2-addrdst-dport_20260311T052747Z`; broader behavior remains `UNVERIFIED`. | YES (fixtures/docs/tracker only) |
| 2026-03-11T06:05:00Z | PAN-OS runtime/query-token reconciliation (`11.0.6-h1`) | Applied minimal adapter query-builder reconciliation: destination-port filter token changed from `port.dst` to `dport` in `PANOSAdapter._build_traffic_query(...)`; added unit test asserting query output includes `addr.dst` + `dport` and excludes `port.dst`. | HIGH | Aligns runtime destination filtering guidance with the existing scenario-scoped real-capture proof (`deny-hit-udp-obsgate-stage1_20260311T052621Z` + `deny-hit-udp-obsgate-stage2-addrdst-dport_20260311T052747Z`) without broad adapter rewrite. No new assumption promotions were made in this run. | YES (adapter/tests/docs/tracker) |
| 2026-03-11T05:27:48Z | PAN-OS bounded observability-gated XML verification (`11.0.6-h1`) | Executed one Stage 1 + one Stage 2 real-capture verification from the completed fresh deny-row record. Stage 1 query (no destination tokens) returned qualifying deny entries (`job 444`, `FIN`, `logs count=20`). Stage 2 query (`addr.dst eq 10.1.20.20` + `dport eq 30053`) also returned matching deny entries (`job 445`, `FIN`, `logs count=20`) for the same signature window. | HIGH | Scenario-scoped promotion now justified for `11.0.6-h1` UDP deny signature: `addr.dst` and `dport` are evidenced under `capture_provenance=real_capture`. No cross-version or broad-token promotion is justified. | YES (fixtures/tests/docs/tracker only) |
| 2026-03-10T21:48:27Z | PAN-OS fresh live deny-row observability record (`11.0.6-h1`) | Recorded fresh live deny row tied to reproduction window: `src=10.1.99.10`, `dst=10.1.20.20`, `dport=30053`, `app=not-applicable`, `action=deny`, `rule=interzone-default`, `session_end_reason=policy-deny`, `type=drop`, `zone.src=management`, `zone.dst=servers`; session-id shown as `78` pending field-label confirmation. | HIGH | Provides the observability baseline required before another bounded XML Stage 1/Stage 2 token-validation run; does not by itself promote any query-token assumption. | YES (docs/tracker only) |
| 2026-03-10T22:55:00Z | PAN-OS guidance reconciliation (`11.0.6-h1`) | Reconciled PAN-OS traffic-log token guidance to canonical port fields (`sport`, `dport`, `natsport`, `natdport`) and corrected default candidate away from `port.dst` to `dport`; destination-address token candidate (`addr.dst`) remains environment/version-specific. | HIGH | Keeps future verification runs aligned to PAN-OS field naming and prevents defaulting to stale token assumptions. No token promotions were made. | YES (docs/tracker only) |
| 2026-03-10T22:40:00Z | PAN-OS UDP deny observability workflow (`11.0.6-h1`) | Added a bounded observability-first checklist template requiring fresh live deny-row confirmation (timestamp/signature/filter/session fields) before any new XML destination-token validation run. No new XML token-validation query was run in this observability-only step. | HIGH | Formalizes blocker handling: when Stage 1A/1B return zero entries, next action is live observability evidence capture, not destination-token promotion. `addr.dst` and destination-port token assumptions remain `UNVERIFIED`. | YES (docs/tracker only) |
| 2026-03-11T01:52:40Z | PAN-OS final bounded UDP verification (`11.0.6-h1`) | Executed exact 60-second UDP generation command to `10.1.20.20:30053`, then ran Stage 1A during active flow (`5m` signature query) and Stage 1B immediately after generation (`15m` signature query). Both captures succeeded submit/poll (`jobs 415, 416`) but returned `logs count="0"`; Stage 2 did not run. | HIGH | Distinguishes current outcome as “deny event not observed in XML results even with active generation + confirmation pass,” not destination-token mismatch. `addr.dst`/`port.dst` remain `UNVERIFIED`. | YES (fixtures/docs/tracker only) |
| 2026-03-11T01:40:32Z | PAN-OS two-stage UDP deny verification (`11.0.6-h1`) | During operator-run bounded UDP generation (`10.1.99.10 -> 10.1.20.20:30053`), Stage 1 used `addr.src=10.1.99.10`, `rule=interzone-default`, `action=deny`, `session_end_reason=policy-deny` and returned submit job `401` with poll `FIN` + `logs count="0"`; Stage 2 (`addr.dst` + `port.dst`) was not run per fail-closed rule. | HIGH | Confirms bounded live generation was attempted but no qualifying deny event was observed in the Stage 1 window; `addr.dst` and `port.dst` remain `UNVERIFIED`. | YES (fixtures/docs/tracker only) |
| 2026-03-11T01:37:35Z | UDP deny verification precondition (`11.0.6-h1`) | Self-contained run could not proceed because non-interactive access to source host `10.1.99.10` failed (`Permission denied` for tested SSH users). Per fail-closed rule, no Stage 1/Stage 2 PAN-OS capture was run in this attempt. | HIGH | Prevents blind capture retries without verified traffic generation path; `addr.dst` and `port.dst` remain `UNVERIFIED` for the UDP signature until bounded traffic can be generated and captured. | YES (docs/tracker only) |
| 2026-03-11T01:27:00Z | PAN-OS two-stage UDP deny verification (`11.0.6-h1`) | Stage 1 (bounded 5m, reproduction-coupled) used `addr.src=10.1.99.10`, `rule=interzone-default`, `action=deny`, `session_end_reason=policy-deny` and returned submit job `390` with poll `FIN` + `logs count="0"`; Stage 2 (`addr.dst eq 10.1.20.20` + `port.dst eq 30053`) was not run per fail-closed rule. | HIGH | No qualifying deny event was captured in the bounded Stage 1 window, so `addr.dst` and `port.dst` remain `UNVERIFIED` for this UDP signature. | YES (fixtures/docs/tracker only) |
| 2026-03-11T01:06:09Z | PAN-OS two-stage ICMP deny verification (`11.0.6-h1`) | Stage 1 used the exact deny signature (`addr.src=10.1.99.3`, `app=icmp`, `rule=interzone-default`, `action=deny`, `session_end_reason=policy-deny`, 5m window) and returned submit job `281` with poll `FIN` + `logs count="0"`; Stage 2 (`addr.dst eq 10.1.20.180`) was not run per fail-closed rule. | HIGH | No qualifying deny event was captured in the bounded Stage 1 window, so `addr.dst` remains `UNVERIFIED`; `port.dst` remains `UNVERIFIED` and out-of-scope for ICMP. | YES (fixtures/docs/tracker only) |
| 2026-03-10T23:13:07Z | PAN-OS two-stage ICMP deny verification (`11.0.6-h1`) | Stage 1 (bounded 5m, reproduction-coupled) used `addr.src=10.1.99.3`, `action=deny`, `session_end_reason=policy-deny` and returned submit job `280` with poll `FIN` + `logs count="0"`; Stage 2 (`addr.dst eq 10.1.20.180`) was not run per fail-closed rules. | HIGH | No qualifying deny event was observed in the bounded Stage 1 window, so `addr.dst` cannot be promoted from this run; `port.dst` remains `UNVERIFIED` and out-of-scope for ICMP proof. | YES (fixtures/docs/tracker only) |
| 2026-03-10T23:09:32Z | PAN-OS two-stage ICMP deny verification (`11.0.6-h1`) | Stage 1 (bounded 5m, reproduction-coupled) used `addr.src=10.1.99.3`, `action=deny`, `session_end_reason=policy-deny` and returned submit job `279` with poll `FIN` + `logs count="0"`; Stage 2 (`addr.dst eq 10.1.20.180`) was not run per fail-closed rules. | HIGH | Confirms no qualifying deny event was observed for the just-reproduced ICMP target in the bounded window; `addr.dst` cannot be promoted from this run and `port.dst` remains out-of-scope/unverified. | YES (fixtures/docs/tracker only) |
| 2026-03-10T22:12:50Z | local PAN-OS bounded deny scenario (`11.0.6-h1`) | Ran one bounded real-capture attempt for operator-confirmed denied ICMP path: `src=10.1.99.3`, `dst=10.1.20.180`, query constrained with `action=deny` and `session_end_reason=policy-deny` over a 15-minute window. Submit succeeded with job `278`; first poll returned `FIN` with `logs count="0"` (no deny/drop/policy-deny entry captured). | HIGH | Confirms this exact denied ICMP target was attempted through healthy read-only submit/poll path, but no qualifying deny-hit evidence was observed in-window; query-token assumptions remain `UNVERIFIED`. | YES (fixtures/docs/tracker only) |
| 2026-03-10T18:48:48Z | local PAN-OS bounded deny scenario (`11.0.6-h1`) | Ran one bounded deny-focused real-capture query via hardened harness: `(action neq allow)` with 6h window. Submit succeeded with job `217`; first poll returned `FIN` with `logs count="0"` (no deny/reset entry). | HIGH | Confirms healthy read-only submit/poll path for this bounded scenario but still provides no deny/reset evidence to validate/promote destination/port query-token assumptions. | YES (fixtures/docs/tracker only) |
| 2026-03-10T18:23:07Z | local PAN-OS bounded deny scenario (`11.0.6-h1`) | Ran one bounded deny-focused real-capture query via hardened harness: `(action neq allow)` with 1h window. Submit succeeded with job `216`; first poll returned `FIN` with `logs count="0"` (no deny/reset entry). | HIGH | Confirms read-only harness path and async submit/poll behavior for this scenario, but does not provide deny-hit evidence needed to validate destination/port query-token assumptions. | YES (fixtures/docs/tracker only) |
| 2026-03-10T17:31:55Z | local PAN-OS real-capture collection | Hardened read-only harness successfully collected `capture_provenance=real_capture` scenario packs for PAN-OS `11.0.6-h1` (`deny-hit`, `no-match`, `metadata-hit`, `query-shape`, `xpath-shape`) using keygen bootstrap; scenarios have mixed completeness (some submit errors or zero-entry polls). | HIGH | Real environment evidence now exists in-repo for version-scoped verification; promotion remains bounded to what each scenario actually proves. | YES (fixtures/tests/docs/tracker/script) |
| 2026-03-10T17:31:18Z | PAN-OS query-shape evidence | Real-capture `query-shape`/`deny-hit` submit responses with `addr.dst eq example.com` returned XML API error code `17` (`addr ... doesn't exist`) and no job-id/poll artifact. | HIGH | Disproves broad assumption that literal destination values are universally valid for current query token usage in this environment; query-token mapping remains `UNVERIFIED` for promotion purposes. | YES (fixtures/tests/docs) |
| 2026-03-10T17:31:55Z | PAN-OS XPath/config shape evidence | Real-capture `metadata-hit` and `xpath-shape` responses for `/config/devices/entry/vsys/entry/rulebase/security/rules` returned `<rules><entry ...>` structure on PAN-OS `11.0.6-h1` with sanitized entry attributes. | MEDIUM | Supports version-scoped partial promotion of base config XPath shape (not full rule-name lookup correctness across versions). | YES (fixtures/tests/docs) |
| 2026-03-10T17:29:25Z | PAN-OS harness URL encoding/sanitization | Harness now URL-encodes dynamic `query` and `xpath` request parameters and additionally sanitizes rule/object-like values (`entry name`, `uuid`, `member`) in saved artifacts. | HIGH | Prevents malformed URL failures during bounded live queries and improves real-capture data hygiene without changing adapter runtime logic. | YES (script/tests/docs) |
| 2026-03-10T05:32:15Z | local PAN-OS real capture attempt | Bounded live harness run with current `.env` credentials reached keygen preflight but failed because response did not contain `<key>` and did not match explicit `403 Invalid Credential` signature; harness stopped before any log/config capture. | HIGH | No new `real_capture` fixture packs were produced; PAN-OS query-token and XPath assumptions remain `UNVERIFIED` until valid API key/auth path is available. | YES (docs/tracker only) |
| 2026-03-09T19:50:48Z | PAN-OS keygen auth-failure precision | Harness now classifies auth guidance only on explicit keygen auth rejection signatures (`403` + `Invalid Credential`), while non-auth XML keygen errors fail fast with generic keygen/API failure messaging; invalid-credential retries remain disabled. | HIGH | Reduces false auth diagnosis and keeps operator triage accurate after confirmed network reachability. | YES (script/tests/docs/tracker only) |
| 2026-03-09T06:16:41Z | PAN-OS local collection auth preflight | Hardened harness now performs fail-fast keygen auth handling: on sanitized XML `Invalid Credential`/auth-error response it stops immediately, prints operator prerequisite guidance (API key preferred, XML API role requirements, external-auth username format), and avoids repeated invalid-credential retry behavior. | HIGH | Makes local real-capture workflow safer and clearer after confirmed network reachability by separating auth blockers from connectivity blockers. | YES (script/tests/docs/tracker only) |
| 2026-03-09T04:53:32Z | local PAN-OS real capture attempt | Connectivity to configured PAN-OS host is now confirmed (DNS resolves to `10.1.99.1`, TCP/443 reachable), but read-only harness keygen returns XML API `403 Invalid Credential`. | HIGH | Collection blocker shifted from network reachability to API auth; still no `real_capture` fixture packs collected in this run. | YES (docs/tracker only) |
| 2026-03-08T23:24:00Z | local PAN-OS real capture attempt | Live read-only harness execution using `.env` PAN-OS credentials failed with connectivity error (`curl (7)` / TCP 443 timeout) to configured host; DNS resolution succeeded but firewall management endpoint was unreachable from run environment. | HIGH | No `real_capture` fixture packs were collected in this run; PAN-OS query-token/XPath assumptions remain `UNVERIFIED`. | YES (docs/tracker only) |
| 2026-03-08T23:13:51Z | PAN-OS local collection safety | Added repo-owned read-only guard helper (`scripts/panos_readonly_guard.sh`) enforcing allowed request classes/actions and rejecting disallowed operations (`set/edit/delete/move/rename/clone/override/commit*`, unknown types/actions). | HIGH | Prevents arbitrary or unsafe PAN-OS calls in local fixture collection workflow by forcing fail-closed allowlist checks. | YES (scripts/tests/docs only) |
| 2026-03-08T23:13:51Z | PAN-OS live harness tests | Added harness tests proving read-only guard rejection/allow behavior and script-produced real-capture manifests/sanitized keygen artifacts under fake-curl execution (`tests/fixtures/test_panos_collection_harness.py`). | HIGH | Locks local collection workflow safety and evidence-trust metadata behavior without changing adapter runtime logic. | YES (tests only) |
| 2026-03-08T23:13:51Z | PAN-OS protocol contract docs | Fixture README now explicitly distinguishes `type=log query=...` monitor filter syntax from `type=config action=get|show|complete xpath=...` semantics and documents local-firewall run pattern with `.env` credentials. | HIGH | Reduces risk of conflating query-token and XPath assumptions during verification runs. | YES (docs only) |
| 2026-03-08T21:53:57Z | PAN-OS fixture trust contract | Versioned fixture packs now require provenance-aware `CAPTURE_METADATA.txt` fields (`capture_provenance`, `verification_scope`, `panos_version_source`, etc.) with strict allowed-value validation in fixture selector helpers/tests. | HIGH | Prevents template/synthetic fixtures from being silently treated as real-environment verification evidence. | YES (fixtures/tests/docs/script only) |
| 2026-03-08T21:53:57Z | PAN-OS selector gating | `select_versioned_capture(...)` now supports trust filters (`require_provenance`, `minimum_verification_scope`) and fails closed when no matching capture satisfies requirements. | HIGH | Real-environment verification tests can require `real_capture`; newest-match selection is constrained to trusted candidates only. | YES (fixtures/tests only) |
| 2026-03-08T21:53:57Z | PAN-OS gather script manifesting | Gather script manifests now include required trust/provenance fields and version-source semantics (`auto_detected`/`override`/`unknown`) while keeping keygen/password material sanitized from persisted logs/manifests. | HIGH | Makes real-firewall captures explicit (`capture_provenance=real_capture`) and reduces risk of trust confusion in future verification workflows. | YES (script/docs/tests only) |
| 2026-03-08T21:37:53Z | PAN-OS versioned fixture test wiring | One existing PAN-OS adapter fixture-alignment test now resolves fixtures via `select_versioned_capture(version=\"11.0.2\", scenario=\"deny-hit\")` and loads `CAPTURE_METADATA.txt` for explicit version/scenario assertions. | HIGH | Establishes canonical end-to-end versioned fixture selection pattern in an existing test without changing adapter behavior. | YES (tests/fixtures only) |
| 2026-03-08T21:26:29Z | PAN-OS fixture selector | Added `tests/fixtures/panos_fixture_selector.py` with version+scenario capture selection (`versions/<panos_version>/<capture_label>_<timestamp>/`) and capture-manifest parsing so tests can load the newest matching scenario automatically. | HIGH | Reduces manual fixture-path wiring in tests and standardizes multi-version fixture selection without adapter logic changes. | YES (tests/helpers only) |
| 2026-03-08T21:26:29Z | PAN-OS fixture collection auth | Fixture gather helper now supports username/password keygen fallback (`type=keygen`) when `--api-key` is not provided; request/response logging remains sanitized and key source is recorded in capture metadata. | HIGH | Improves usability for environments where API keys are not pre-provisioned while preserving fixture sanitization and conservative behavior. | YES (script/docs only) |
| 2026-03-08T20:14:17Z | PAN-OS fixture collection helper | `scripts/gather_panos_fixtures.sh` now produces versioned fixture packs (`versions/<panos_version>/<capture_label>_<timestamp>/`), captures system-info version metadata, records sanitized request logs, and mirrors canonical required fixture XML files used by validation tests. | HIGH | Improves repeatable multi-version evidence capture while keeping adapter behavior unchanged and conservative. | YES (script/docs/tests only) |
| 2026-03-08T20:14:17Z | fixture-pack contract | Fixture README now defines versioned capture layout, capture matrix guidance (deny/no-match/metadata/malformed samples), and explicit script workflow for safe future evidence collection. | HIGH | Clarifies how to build a reusable fixture corpus across PAN-OS versions without treating redacted values as production truth. | YES (docs/tests only) |
| 2026-03-08T10:40:30Z | PAN-OS fixture verification | Fixture-based validation confirms current adapter parser markers align with fixture XML shape: submit job extraction via `.//job`, poll status via `.//status`, log entries via `.//logs/entry`, metadata entry via `.//entry[@name=...]`. | MEDIUM | Confirms parser shape assumptions against sanitized fixture structure without changing runtime behavior. | YES (tests/docs only) |
| 2026-03-08T10:40:30Z | sanitization contract | PAN-OS fixture-pack README now explicitly requires redaction/tokenization for IPs/usernames/hostnames/serials/device names/internal objects/rule names/ticket refs/auth material, plus XML-structure preservation and placeholder consistency rules. | HIGH | Reduces risk of unsafe capture commits and provides clear operator guidance for future evidence collection. | YES (docs only) |
| 2026-03-08T10:40:30Z | fixture alignment tests | Added PAN-OS adapter fixture-alignment tests and strengthened fixture-pack validation test for sanitization-contract presence. | HIGH | Creates a durable harness for future real-capture drop-ins while preserving conservative no-guess behavior. | YES (completed in this run) |
| 2026-03-08T10:17:36Z | PAN-OS fixture scaffolding | Added `docs/fixtures/panos_verification/` fixture pack templates with required sample file names for traffic-log submit, traffic-log poll, and rule-metadata config responses, plus explicit sanitization and expected-field guidance. | HIGH | Creates a stable capture target for future real-environment PAN-OS evidence collection without changing adapter behavior or claiming verified mappings. | YES (completed in this run) |
| 2026-03-08T10:17:36Z | test scaffolding | Added fixture-pack validation test to load/parse required PAN-OS XML sample files and check minimum structural markers (`job`, `status`, `logs/entry`, `entry`). | HIGH | Ensures future evidence drop-ins stay parseable and structurally usable for validation work before adapter changes are considered. | YES (completed in this run) |
| 2026-03-08T10:03:08Z | API/UI unknown explainability | Unknown verdict responses now surface compact explainability via existing confidence fields plus additive `unknown_reason_signals`; UI renders a minimal "Why unknown" section tied to `path_confidence` and `evidence_completeness`. | HIGH | Improves operator clarity for `unknown` outcomes while preserving verdict authority semantics (`denied` still requires authoritative evidence). | YES (completed in this run) |
| 2026-03-08T10:03:08Z | persisted result loading | `_load_result_record` now coerces malformed/missing persisted confidence values into safe bounded defaults and derives unknown-reason signals from summary/readiness/confidence when explicit signals are absent. | HIGH | Prevents malformed persisted confidence data from breaking API/UI rendering and keeps unknown explainability available without classifier changes. | YES (completed in this run) |
| 2026-03-08T09:45:42Z | PAN-OS validation pass | Repository/config/docs inspection found no target PAN-OS version artifact, no sanitized real XML traffic-log request/response sample, and no environment-specific rulebase XPath evidence to validate current placeholder field/XPath assumptions. | HIGH | No evidence-backed adapter mapping changes were justified; `addr.dst`/`port.dst` and metadata XPath remain explicitly `UNVERIFIED` placeholders. | YES (docs/comments only) |
| 2026-03-08T09:34:55Z | integration tests | Lifecycle integration now asserts persisted PAN-OS deny metadata through API submit -> queue -> worker -> persistence -> `GET /api/v1/requests/{id}/result` -> `/requests/{id}` rendering, without route-loader patching. | HIGH | Proves metadata survives normal DB-backed retrieval/render path and removes reliance on route-mock-only confidence for this behavior. | YES (completed in this run) |
| 2026-03-08T09:34:55Z | integration tests | Malformed PAN-OS metadata in deny evidence now has persisted-lifecycle coverage: verdict remains deny, API returns detail, and UI skips metadata panel cleanly. | HIGH | Locks graceful degradation semantics end-to-end so metadata shape problems do not break operator workflows or alter authority. | YES (completed in this run) |
| 2026-03-08T09:25:11Z | API/UI result output | Result page now renders a minimal PAN-OS rule metadata panel (`rule_name`, optional action/description/disabled/tags) only when persisted observed-fact detail indicates authoritative PAN-OS deny evidence and includes `detail.rule_metadata`. | HIGH | Makes PAN-OS deny explainability visible to operators without changing classifier/verdict authority semantics or adding new API fields. | YES (completed in this run) |
| 2026-03-08T09:25:11Z | route tests | Added route/UI coverage for PAN-OS metadata present, absent, and malformed cases; malformed metadata no longer risks template/render failures and is skipped cleanly. | HIGH | Locks conservative operator-output behavior so missing/bad metadata does not break result retrieval/rendering and does not become authority. | YES (completed in this run) |
| 2026-03-08T09:25:11Z | API output contract | `GET /api/v1/requests/{id}/result` continues to expose PAN-OS metadata via existing `observed_facts[].detail.rule_metadata` when present; no top-level result schema expansion was required. | HIGH | Preserves thin additive contract and avoids broad result-schema redesign while still surfacing metadata. | YES (tests/docs in this run) |
| 2026-03-08T09:04:13Z | PAN-OS adapter | `lookup_rule_metadata(...)` now performs conservative optional PAN-OS XML config lookup and returns minimal parsed fields (`rule_name`, optional action/description/disabled/tags/device_host/vsys) when present. | MEDIUM | Improves deny explainability while preserving deny authority behavior because metadata remains enrichment-only. | YES (completed in this run) |
| 2026-03-08T09:04:13Z | PAN-OS adapter | Metadata lookup failures (timeout/request error/malformed/no-match) now degrade gracefully to empty metadata; deny evidence path remains intact. | HIGH | Prevents metadata retrieval instability from breaking the core authoritative deny path. | YES (completed in this run) |
| 2026-03-08T09:04:13Z | tests | PAN-OS adapter tests now cover metadata lookup success/no-match/malformed/timeout and verify deny evidence still returns when metadata lookup fails. | HIGH | Locks optional explainability enrichment semantics without weakening existing authoritative deny gating. | YES (completed in this run) |
| 2026-03-08T08:50:00Z | integration tests | Added integration-style lifecycle coverage (`tests/fixtures/test_lifecycle_integration.py`) proving submit -> queue enqueue -> worker dequeue/dispatch -> persistence -> API result retrieval for both authoritative PAN-OS deny and no-authoritative-evidence paths. | HIGH | Confirms end-to-end persisted lifecycle behavior for deny/non-deny outcomes without real vendor calls, reducing risk of regressions in queue/persist/report path. | YES (completed in this run) |
| 2026-03-08T08:05:39Z | authoritative_correlation | Step now filters PAN-OS evidence conservatively: only records with `source=panos`, `normalized.authoritative=true`, and `normalized.action=deny` are forwarded as authoritative evidence. | HIGH | Prevents non-deny/malformed PAN-OS outputs from influencing deny decisions while preserving worker-only vendor boundary. | YES (completed in this run) |
| 2026-03-08T08:05:39Z | tests | Added `tests/unit/test_authoritative_correlation.py` to prove PAN-OS deny inclusion and exclusion of non-deny, malformed, timeout, and no-match adapter outputs; also verifies absent PAN-OS authoritative evidence classifies as `unknown`. | HIGH | Locks conservative PAN-OS consumption behavior at step level and guards against regressions that could create weak deny signals. | YES (completed in this run) |
| 2026-03-08T07:37:54Z | PAN-OS adapter | `PANOSAdapter.query_evidence` now performs XML traffic-log async job submission (`type=log`, `log-type=traffic`) and polling (`action=get`, `job-id`) per configured firewall host, and returns evidence only for deny/reset actions. | MEDIUM | Provides the adapter-side PAN-OS deny evidence path consumed by authoritative-correlation. | YES (completed in this run) |
| 2026-03-08T07:37:54Z | PAN-OS adapter | PAN-OS adapter now handles timeout, no-match, HTTP errors, and malformed XML conservatively by returning no authoritative deny matches instead of forcing evidence. | HIGH | Preserves MVP invariant (`unknown` over weak certainty) when upstream log responses are incomplete/unreliable. | YES (completed in this run) |
| 2026-03-08T07:37:54Z | tests | Added `tests/adapters/test_panos_adapter.py` to cover PAN-OS XML job flow for success, timeout, no-match, and malformed XML; updated adapter contract test to validate list output without stub assumptions. | HIGH | Locks adapter behavior to conservative authority semantics and prevents regression to stub-only response assumptions. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | API entrypoint is `am_i_blocked_api:app` in `services/api/am_i_blocked_api/__init__.py`; there is no `services/api/main.py`. | HIGH | Updated `REPO_MAP.md` entrypoint section to prevent incorrect launch assumptions. | NO |
| 2026-03-08T00:00:00Z | repo | Historical note (superseded later 2026-03-08): worker process was a heartbeat placeholder and Redis dequeue loop was TODO at that time. | HIGH | Historical context only; superseded by later verification entries showing queue consumption implementation. | NO |
| 2026-03-08T00:00:00Z | repo | Bounded probe helpers currently catch timeout/error paths and return structured failure payloads; pipeline/classifier falls back to `unknown` when no authoritative deny evidence exists. | HIGH | Added tests to lock this safety behavior and prevent regressions. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | Shared infra checks now exist in core (`check_database_readiness`, `check_redis_readiness`) and are used by API readiness and worker startup logging. | HIGH | API/worker can report DB+Redis availability explicitly without vendor coupling. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | API `/api/v1/readyz` returns structured dependency status (`ok` or `degraded`) based on Postgres/Redis checks. | HIGH | Readiness reporting now reflects queue/DB health requirement from MVP guardrails. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | API request routes now use Postgres-first persistence/lookup (`RequestRow`/`ResultRow`) with explicit in-memory fallback when DB operations fail. | HIGH | Moves lifecycle toward persisted workflow without breaking local operation before queue/worker wiring is complete. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | API submit now enqueues jobs to Redis queue key `am_i_blocked:jobs`; worker main loop now dequeues Redis jobs and dispatches `run_diagnostic`. | HIGH | Core async API->queue->worker dispatch path now exists; remaining gap is durable worker result persistence. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | Worker `persist_and_report` now persists completion state to Postgres (`requests.status=complete`) and upserts `result` rows including `report_json` evidence bundle. | HIGH | API durable result reads can now resolve completed diagnostics from DB rather than memory-only state. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | API and worker runtime now operate DB-first without in-memory request/result fallback; persistence dependency failures are surfaced as explicit degraded (`503`) responses. | HIGH | DB-backed request/result state is now the operational single source of truth for the MVP flow. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | Failed request transitions now write reason metadata into `audit.params_json` (`action=request_failed`) and API/UI expose the latest reason as `failure_reason`. | HIGH | Failed states are now actionable without requiring immediate worker log access. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | Failed request transitions now persist structured audit metadata (`reason`, `stage`, `category`) and API/UI expose all three fields for triage (`failure_reason`, `failure_stage`, `failure_category`). | HIGH | Failed-state diagnosis is clearer and no longer depends on only free-form error text. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | Failure metadata now uses bounded enums (`FailureStage`, `FailureCategory`); worker tags failures with concrete pipeline step names (for example `validate_request`, `authoritative_correlation`), and API normalizes unknown values to `unknown`. | HIGH | Failure triage fields are now consistent and safe for downstream UI/automation filtering. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | UI failed-state rendering now maps normalized `failure_stage`/`failure_category` to compact first-hop triage actions, while API outputs remain raw normalized values. | HIGH | Operators get immediate triage guidance without changing API response contracts. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | LogScale | LogScale stub evidence is now explicitly marked `classification_role=enrichment_only_unverified` with `authoritative=false`; payload remains non-authoritative and does not provide deny-driving fields in stub mode. | HIGH | Reinforces MVP rule that LogScale is enrichment-only until verified query behavior is implemented. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | Source readiness step now has explicit unit coverage for LogScale configured and not-configured branches. | HIGH | Prevents regressions in readiness reporting for LogScale source availability. | YES (completed in this run) |
| 2026-03-08T05:51:03Z | LogScale | Vendor KB now explicitly documents current repo LogScale behavior as `UNVERIFIED`: readiness is lightweight repository reachability and evidence output is enrichment-only normalized stub metadata. | HIGH | Reduces agent drift by aligning vendor-grounding docs with current code/test behavior without promoting LogScale to deny authority. | YES (docs only) |
| 2026-03-08T00:00:00Z | repo | Classifier deny authority now requires authoritative sources (PAN-OS/SCM) for deny/decrypt-deny paths; LogScale enrichment fields, including malformed deny-like content, cannot independently produce `denied`. | HIGH | Protects MVP invariant: enrichment telemetry cannot override authoritative deny logic. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | LogScale | Classifier now emits explicit observed-fact entries for `enrichment_only_unverified` LogScale records, documenting they are excluded from deny authority decisions. | HIGH | Result bundles now communicate enrichment-vs-authoritative separation directly to operators. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | Result UI now renders observed-fact badges (`Authoritative signal` vs `Enrichment only`) based on observed-fact detail metadata (`classification_role`, `authoritative`). | HIGH | Operators can distinguish authoritative policy evidence from enrichment context at a glance. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | API docs now explicitly define observed-fact detail metadata keys consumed by UI labeling: `classification_role` and `authoritative`, including enrichment-only interpretation rules. | HIGH | Establishes a stable contract for UI/API consistency without changing verdict authority logic. | YES (completed in this run) |
| 2026-03-08T00:00:00Z | repo | API docs now include `RequestDetail` failed-state examples with `failure_reason`, `failure_stage`, and `failure_category`, plus triage interpretation guidance. | HIGH | Makes failed-request troubleshooting fields explicit for integrators and operators. | YES (completed in this run) |
| 2026-03-08T06:02:00Z | repo | API now exposes `GET /api/v1/requests/{request_id}/result/evidence-bundle` with attachment headers; result page download control now points to this endpoint for direct JSON evidence-bundle download. | HIGH | Makes operator evidence export explicit and consistent across browsers while preserving existing result authority semantics. | YES (completed in this run) |
| 2026-03-08T06:23:18Z | repo | Worker pipeline removed remaining in-memory request/result hooks (`request_store`/`result_store`); step-7 persistence failure now raises and marks request failed instead of returning a non-persisted result. | HIGH | Enforces true API -> Redis -> worker -> Postgres lifecycle semantics and prevents false-positive completion when persistence is unavailable. | YES (completed in this run) |

## Open Verification Needs

- Promoted from `UNVERIFIED` (version-scoped):
  - PAN-OS `11.0.6-h1`: base config show XPath `/config/devices/entry/vsys/entry/rulebase/security/rules` returns `<rules><entry ...>` structure in real captures (`metadata-hit_20260310T173041Z`, `xpath-shape_20260310T173154Z`).
  - PAN-OS `11.0.6-h1` UDP deny signature (observability-coupled): `addr.dst` and `dport` matched in real-capture Stage 2 after qualifying Stage 1 capture (`deny-hit-udp-obsgate-stage1_20260311T052621Z`, `deny-hit-udp-obsgate-stage2-addrdst-dport_20260311T052747Z`).
- PAN-OS XML log-job request/response shapes for target firewall versions: `UNVERIFIED`.
- PAN-OS destination-token behavior outside the proven UDP deny signature scope remains `UNVERIFIED` (including other apps/protocols/windows and cross-version behavior).
- Latest distinct-scenario orchestrator attempt (`deny-hit-tcp-distinct-observe-validate-stage1_20260311T073259Z`) produced `observability_hit=false` in `VALIDATION_RESULT.json`; no Stage 2 token evidence exists from this run.
- Latest distinct-scenario retry with improved correlation input (`deny-hit-tcp-distinct-observe-validate-stage1_20260311T151256Z`) also produced `observability_hit=false`; no new token evidence exists and no Stage 2 token subqueries ran.
- Latest distinct-scenario retry using ready `OBSERVABILITY_INPUT.json` (`deny-hit-tcp-distinct-observe-validate-obsinput-stage1_20260311T164815Z`) also produced `observability_hit=false`; no new token evidence exists and no Stage 2 token subqueries ran.
- PAN-OS traffic-log canonical port field names for guidance are `sport`, `dport`, `natsport`, `natdport`; environment-specific query validation for these fields remains incomplete.
- PAN-OS XML config XPath used for rule metadata lookup may vary by PAN-OS version/device schema and remains `UNVERIFIED`.
- PAN-OS metadata field completeness/semantics (for example tags/disabled/action normalization) remain intentionally minimal and `UNVERIFIED` beyond tested subset.
- Fixture-level parser shape alignment is now verified, but fixture values remain non-authoritative placeholders unless replaced by sanitized real captures.
- Evidence needed to promote remaining PAN-OS assumptions from `UNVERIFIED`:
  - ready `OBSERVABILITY_INPUT.json` with materially stronger correlation evidence (prefer session ID + exact UI filter string and structured row fields) for any repeated-no-hit signature-family retry
  - latest run `OBSERVABILITY_RECORD.json` showing a qualifying Stage 1 observability hit for the target signature (manual template evidence is optional supplemental input, not mandatory)
  - target PAN-OS version/build identifier(s) for queried devices
  - additional sanitized XML API traffic-log submit + `action=get` poll responses that include known deny hits for other scenario scopes (current Stage 1/Stage 2 proof is limited to one UDP deny signature for `11.0.6-h1`)
  - sanitized XML config response for known rule metadata lookup path (including vsys/rulebase context) from target environment with explicit single-rule lookup path proof
- Repeated no-hit signature families now require materially stronger observability correlation evidence before rerun via ready `OBSERVABILITY_INPUT.json`; blind distinct-signature retries are no longer the next-step default.
- One improved-correlation run for distinct family `src=10.1.99.3,dst=10.1.20.21,dport=30053,app=not-applicable,rule=interzone-default,session_end_reason=policy-deny` has now been consumed under ready observability input and still no-hit; do not rerun this family again without materially newer/stronger evidence.
- Selector decision artifacts (`NEXT_CANDIDATE_DECISION.json`/`.md`) are now the required source for choosing future PAN-OS live candidate families; ad hoc prompt drift is not the run-selection path.
- Current selector output classifies the distinct TCP family `10.1.99.3|10.1.20.21|30053|not-applicable|unknown|interzone-default|policy-deny|ssh_custom_command` as `exhausted_pending_new_evidence` and no-retry without materially stronger/newer correlation evidence.
- Current selector output primary recommendation is to pause PAN-OS token expansion because no worthwhile non-exhausted family meets destination-token candidate criteria with practical observability potential.
- Project framing update: repeated zero-entry real-capture deny attempts shifted PAN-OS token work from "query-token mismatch" to "observability first"; no additional `UNVERIFIED` promotions were justified in this run.
- This run executed one bounded observability-gated XML verification pair and promoted only scenario-scoped `addr.dst`/`dport` claims backed by `real_capture`.
- Latest self-contained prerequisite gap for UDP verification: non-interactive execution path to source host `10.1.99.10` was unavailable in this run, so bounded local traffic generation could not be initiated directly from the current shell.
- Latest missing prerequisite: working API auth path that returns a valid key (`--api-key` preferred, or keygen response containing `<key>`) so bounded real-capture scenarios can execute at all.
- SCM/Prisma tenant-approved inbound query endpoints and schemas: `UNVERIFIED`.
- SD-WAN API bootstrap/profile call requirements in this tenant: `NEEDS_CONFIRMATION`.
- LogScale role in MVP deny authority (enrichment vs authoritative source): `UNVERIFIED`.
- Torq trigger payload + status polling contract for this environment: `UNVERIFIED`.
